/**
 * 1x1卡片组件使用指南
 *
 * 本组件是一个完整的Vue组件,可直接在Vue应用中使用
 */

// ========================================
// 使用方法
// ========================================

/**
 * 方法一: 直接在HTML中使用 (推荐)
 *
 * 1. 在index.html中引入组件:
 *    <script src="cards/1x1/1x1-component.js"></script>
 *
 * 2. 在Vue模板中使用:
 *    <card-1x1
 *        name="客厅灯"
 *        icon="💡"
 *        stateEntity="light.living_room"
 *        controlEntity="light.living_room"
 *        deviceType="light"
 *        layoutType="default">
 *    </card-1x1>
 */

// ========================================
// Props 参数说明
// ========================================

/**
 * @param {String} name - 设备名称 (必填)
 * @param {String} icon - 卡片图标emoji (必填)
 * @param {String} stateEntity - 状态实体ID (必填)
 * @param {String} controlEntity - 控制实体ID (可选,默认同stateEntity)
 * @param {String} deviceType - 设备类型 (可选,默认'switch')
 *   可选值: 'light', 'switch', 'vacuum', 'feeder', 'display', 'url'
 * @param {String} layoutType - 布局类型 (可选,默认'default')
 *   可选值: 'default', 'icon-only'
 * @param {String} description - 设备描述 (可选)
 * @param {Number} span - 卡片跨度 (可选,默认1)
 */

// ========================================
// 布局类型示例
// ========================================

/**
 * default - 默认布局 (图标+名称+状态)
 *   适用于: 大多数设备
 *   显示内容: 图标(32px)、设备名、状态文字
 *
 * icon-only - 仅图标布局
 *   适用于: 快速操作按钮
 *   显示内容: 仅图标(48px)
 */

// ========================================
// 设备类型示例
// ========================================

// 灯具设备
/*
<card-1x1
    name="客厅灯"
    icon="💡"
    stateEntity="light.living_room"
    deviceType="light"
    layoutType="default">
</card-1x1>
*/

// 开关设备
/*
<card-1x1
    name="热水器"
    icon="🚿"
    stateEntity="switch.water_heater"
    deviceType="switch"
    layoutType="default">
</card-1x1>
*/

// 扫地机
/*
<card-1x1
    name="扫地机"
    icon="🤖"
    stateEntity="vacuum.roborock"
    deviceType="vacuum"
    layoutType="icon-only">
</card-1x1>
*/

// 投喂器
/*
<card-1x1
    name="投喂器"
    icon="🐾"
    stateEntity="counter.pet_feeder"
    deviceType="feeder"
    layoutType="icon-only">
</card-1x1>
*/

// 显示型设备 (冰箱温度)
/*
<card-1x1
    name="冰箱温度"
    icon="🧊"
    stateEntity="sensor.fridge_temp"
    deviceType="display"
    layoutType="default">
</card-1x1>
*/

// ========================================
// 依赖要求
// ========================================

/**
 * 使用本组件需要:
 * 1. Vue 3.x
 * 2. 全局对象 window.app (包含设备控制方法)
 * 3. Vant UI库 (可选,用于Toast提示)
 *
 * window.app 应该包含:
 * - getDeviceState(entityId) - 获取设备状态
 * - handleDeviceClick(device) - 处理设备点击
 * - callService(domain, service, data) - 调用HA服务
 */

// ========================================
// CSS样式
// ========================================

/**
 * 组件会自动引入以下CSS文件:
 * - cards/1x1/css/1x1-card.css
 *
 * 样式特点:
 * - 毛玻璃效果
 * - 正方形卡片 (1:1比例)
 * - 响应式设计
 * - 平滑动画
 */

// ========================================
// 事件说明
// ========================================

/**
 * @event click - 卡片点击事件
 * 组件内部会自动处理设备控制,无需额外绑定事件
 *
 * @event device-state-update - 状态更新事件
 * 全局事件,当设备状态变化时触发
 */

// ========================================
// 常见问题
// ========================================

/**
 * Q: 组件显示"不可用"?
 * A: 检查 stateEntity 是否正确,确保设备已添加到HA
 *
 * Q: 点击卡片无反应?
 * A: 确保 window.app.handleDeviceClick 方法存在
 *
 * Q: 卡片不是正方形?
 * A: 确保引入了1x1-card.css样式文件
 */

// ========================================
// 导出说明
// ========================================

/**
 * 组件支持多种导出方式:
 * 1. CommonJS - module.exports
 * 2. ES6 Module - export default
 * 3. 全局注册 - Vue.component('card-1x1', Component)
 */
